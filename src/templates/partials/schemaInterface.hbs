{
    properties: {
{{#if extends}}
    {{#each extends}}
        ...${{{this}}}.properties,
    {{/each}}
{{/if}}
{{#if properties}}
    {{#each properties}}
        {{{name}}}: {{>schema}},
    {{/each}}
{{/if}}
    },
{{#if isReadOnly~}}
    isReadOnly: {{{isReadOnly}}},
{{else}}
    isReadOnly: false,
{{/if}}
{{#if isRequired~}}
    isRequired: {{{isRequired}}},
{{else}}
    isRequired: false,
{{/if}}
{{#if isNullable~}}
    isNullable: {{{isNullable}}},
{{/if}}
}
